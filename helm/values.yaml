replicaCount: 1

image:
  repository: docker.io/serkannnaml/python-app
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

env:
  username: myuser
  password: mypass
  configMessage: Hello from ConfigMap!
